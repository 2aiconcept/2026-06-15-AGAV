// Surface publique de la lib `@mini-crm/contacts/data-access`.
// Vide pour l'instant : la feature contacts est un squelette ; le `ContactService`
// (signals + HttpClient, sur le modèle de `CompanyService`) sera ajouté ici et exporté.
export {};
