// Surface publique de la lib `@mini-crm/contacts/util`.
// Vide pour l'instant : la feature contacts est un squelette ; le modèle `Contact`
// (interface + `ContactPayload`, sur le modèle de `Company`) sera ajouté ici et exporté.
export {};
