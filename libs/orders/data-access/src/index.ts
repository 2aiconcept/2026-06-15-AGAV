// Surface publique de la lib `@mini-crm/orders/data-access`.
// Vide pour l'instant : la feature orders est un squelette ; le `OrderService`
// (signals + HttpClient, sur le modèle de `CompanyService`) sera ajouté ici et exporté.
export {};
