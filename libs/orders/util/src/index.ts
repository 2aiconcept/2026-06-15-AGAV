// Surface publique de la lib `@mini-crm/orders/util`.
// Vide pour l'instant : la feature orders est un squelette ; le modèle `Order`
// (interface + `OrderPayload`, sur le modèle de `Company`) sera ajouté ici et exporté.
export {};
